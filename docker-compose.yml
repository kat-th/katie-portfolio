services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    image: katie-portfolio:latest
    container_name: katie-portfolio
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
